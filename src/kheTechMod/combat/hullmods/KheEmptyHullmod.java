//DO NOT TOUCH THIS FUCKING FILE IT IS FOR MY SHITTYASS MEMORY.

package kheTechMod.combat.hullmods;

import org.apache.log4j.Logger;

import com.fs.starfarer.api.combat.BaseHullMod;

@SuppressWarnings("unused")
public class KheEmptyHullmod extends BaseHullMod {
    private final Logger log = Logger.getLogger(KheEmptyHullmod.class);
//    from basehullmod, about the class as a whole:
//    Note: the effect class is instantiated once per application session. //remember this part, dingus. EFFECT CLASS IS INSTANCIATED ONCE.
//    Storing campaign data in data members of an implementing class is a bad idea (will likely cause memory leaks), use SectorAPI.getPersistentData() instead.
}
